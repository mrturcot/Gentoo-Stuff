## Enabling GCC LTO
# "-flto=$(12)" refers to the amount of threads used during LTO, one option is to be set to the value of $(nproc)
CFLAGS="-flto=12 -march=znver1 -O2 -pipe -fomit-frame-pointer"
CXXFLAGS="${CFLAGS}"

CC="gcc"
CXX="g++"
AR="gcc-ar"
NM="gcc-nm"
RANLIB="gcc-ranlib"
