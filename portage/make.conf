# These settings were set by the catalyst build script that automatically
# built this stage.
# Please consult /usr/share/portage/config/make.conf.example for a more
# detailed example.

# GCC settings
COMMON_FLAGS="-march=znver1 -O2 -pipe -fomit-frame-pointer"
CFLAGS="${COMMON_FLAGS}"
CXXFLAGS="${COMMON_FLAGS}"
FCFLAGS="${COMMON_FLAGS}"
FFLAGS="${COMMON_FLAGS}"
MAKEOPTS="-j12"

# Gentoo (Portage)
FEATURES="parallel-fetch parallel-install"
GENTOO_MIRRORS="http://gentoo.mirrors.tera-byte.com/"
L10N="en"
PORTAGE_NICENESS=19
PORTAGE_LOGDIR="/var/log/portage"
PORTAGE_ELOG_SYSTEM="save"
PORTAGE_ELOG_CLASSES="warn error info log qa"
VIDEO_CARDS="amdgpu radeonsi"

# USE="Flags"
USE="-systemd -radeon -wayland -cups -smartcard -css -a52 -kde \
     x gnome gtk gtk4 gdm bluetooth alsa pulseaudio pipewire \
     vulkan vaapi x265 dri drm lm-sensors fuse dbus samba \
     clang lto pgo networkmanager unicode vim-syntax mtp \
     qt5 lame flac llvm-libunwind"

# NOTE: This stage was built with the bindist Use flag enabled
PORTDIR="/var/db/repos/gentoo"
DISTDIR="/var/cache/distfiles"
PKGDIR="/var/cache/binpkgs"

# This sets the language of build output to English.
# Please keep this setting intact when reporting bugs.
LC_MESSAGES=C

# Other
INPUT_DEVICES="joystick libinput"
GRUB_PLATFORMS="efi-64"
QEMU_SOFTMMU_TARGETS="alpha aarch64 arm i386 m68k mips mips64 \
     mips64el mipsel ppc ppc64 s390x sh4 sh4eb sparc sparc64 x86_64"
QEMU_USER_TARGETS="alpha aarch64 arm armeb i386 m68k mips mipsel \
     ppc ppc64 ppc64abi32 s390x sh4 sh4eb sparc sparc32plus sparc64"